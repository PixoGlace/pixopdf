from pathlib import Path

from pixopdf.config import APP_NAME as APP_NAME
from pixopdf.config import ORGANIZATION as ORGANIZATION

SUPPORTED_PDF_FILTER = "PDF (*.pdf)"
LOG_DIR = Path.home() / ".pixopdf" / "logs"
